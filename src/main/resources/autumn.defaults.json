{
	// this file is more than just documentation
	// it IS read at runtime and populate the DEFAULT autumn config
	// the user's autumn.json values will be overlayed ontop of these

	// what packages to scan for @Path resources
	// no default currently set
	// all packages in 1 string, seperate packages with ';'
	// TODO support an array here
	pathPackage: 'com.example.app.rest',
	entityPackage: 'com.example.app.pojo',
	
	// do you want to map POJOs to JSON and back?
	// default: true
	pojoMapping: true,
	
	// your JDBC connection information
	jdbc: {
		driver: 'java.sql.Driver',
		url:'jdbc:',
		user:'mruser',
		password:'sup3rs3cr3tp@$$w0rd'
	}
	
}